# Logo Assets

Place your downloaded logo images in this folder and update the `logoSources`
array inside `script.js` with the matching filenames and alt text. Transparent
PNG or SVG files with similar heights work best for the auto-sliding carousel.

